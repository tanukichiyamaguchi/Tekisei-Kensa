# M2 実プロジェクト確認の記録（2026-09-23）

基本設計 08 §2.3 の完了条件と §3.3.4（09 §6.4）の実装時確認のうち、実際の Firebase プロジェクトでしか確かめられない項目の結果です。プロジェクトは `tekisei-kensa-697c4` の 1 つだけで運用します（10 K-13）。

## 1. ルール・インデックスのデプロイ（`firebase-deploy`、H-01 の一部）

| 項目 | 結果 |
|---|---|
| 実行 | GitHub Actions `firebase-deploy` 実行番号 1（`main`、`eed0d5a`）。Environment `production` の承認後に実行 |
| ルール | `firestore.rules`（全拒否）のコンパイルと公開に成功 |
| インデックス | `firestore.indexes.json`（複合 14 本・単一フィールド除外 12 件）のデプロイに成功 |
| CI 用サービスアカウントのロール | Firebase ルール管理者、Cloud Datastore インデックス管理者、Service Usage 閲覧者の 3 つで不足なし（09 §6.4 の 34） |
| `GOOGLE_APPLICATION_CREDENTIALS` の認識 | Firebase CLI 15.30.2 が認識した（09 §6.4 の 33） |
| `--config firebase/firebase.json` での `.firebaserc` の解決 | 別名 `production` が `tekisei-kensa-697c4` に解決された（09 §6.4 の 32） |

## 2. 実装時確認（`firebase-ops` の `verify` = `pnpm firebase:verify`）

実行: GitHub Actions `firebase-ops` 実行番号 1（task = verify）。一時ユーザー 1 件と一時コレクション `_verifyFirebase` の文書は終了時に削除した。

| 09 §6.4 | 項目 | 結果 |
|---|---|---|
| 1 | `where("deletedAt", "==", null)` が null を明示した文書だけに一致する（フィールド欠落の文書は一致しない） | 成立 |
| 2 | `select()` で map フィールド全体が射影される（倍精度も不変） | 成立 |
| 3 | 等価条件だけの複合クエリ（3 条件）は複合インデックスなしで実行できる | 成立 |
| 3 | 未定義の複合インデックスが必要なクエリ（等価 + 並び替え）は `FAILED_PRECONDITION` で失敗する（本番がインデックスを強制することの対照） | 成立 |
| 3 | デプロイ済みのインデックスでリポジトリの全クエリ（Q1〜Q14。利用履歴の昇順を含む）が実行できる | 成立（14 件） |
| 4 | `createSessionCookie` は 7 日・14 日で発行でき、14 日 + 1 秒は `auth/invalid-session-cookie-duration` で拒否される | 成立 |
| 6 | `verifySessionCookie(cookie, true)` が失効後（`auth/session-cookie-revoked`）・無効化後（`auth/user-disabled`）の Cookie を拒否する | 成立 |
| 7 | 自己登録（クライアントの `accounts:signUp`）が `ADMIN_ONLY_OPERATION` で拒否される | 成立（Authentication → 設定 → ユーザー アクションで「作成（登録）を有効にする」をオフ） |
| 7 | Admin SDK の `createUser` は自己登録の無効化の影響を受けない | 成立 |
| 23 | ID トークンの `auth_time` は Unix 秒 | 成立 |
| 24 | セッション Cookie に `email` クレームが含まれる | 成立 |

## 3. 依頼主が行った設定

- GitHub Environment `production`（必須レビュアー: 依頼主）、Secret `FIREBASE_SERVICE_ACCOUNT_KEY`・`FIREBASE_CI_SERVICE_ACCOUNT_KEY`、Variable `NEXT_PUBLIC_FIREBASE_PROJECT_ID`・`NEXT_PUBLIC_FIREBASE_API_KEY`
- 組織ポリシーのプロジェクト単位の例外（10 K-14）
- ウェブアプリ `tekisei-kensa-web` の登録
- Authentication: メール／パスワードを有効化、自己登録を無効化

## 4. 未実施（後続で行う）

- 初期オーナーの作成（`firebase-ops` の `create-owner`）と、ログイン → `POST /auth/session` の確認（Vercel へのデプロイ後）
- API キーの利用制限（HTTP リファラー）と Authentication の承認済みドメインへの本番ドメインの追加（01 §5.3。Vercel の URL 確定後）
